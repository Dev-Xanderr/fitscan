VITE_PUBLIC_URL=https://dev-xanderr.github.io/fitscan

# Supabase REST endpoint for booth lead capture. Public-by-design: the anon
# key (set as a CI secret, not committed) is gated by RLS so the worst a leak
# can do is insert another lead row. See supabase/migrations/ for schema.
VITE_SUPABASE_URL=https://dllsoplwcibepqlcigtu.supabase.co
